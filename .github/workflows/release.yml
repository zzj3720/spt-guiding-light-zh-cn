name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build-release:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build release zip
        shell: pwsh
        run: .\scripts\build_release.ps1 -Version $env:GITHUB_REF_NAME

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: GuidingLight-zh-cn-${{ github.ref_name }}
          path: release/*.zip
